// add whatever parameters you deem necessary
function twoArrayObject(keys, vals) {
  const arrObj = {}
  for (let key in keys) {
    let objKey = keys[key]
    let objVal = vals[key]
    arrObj[key] = 
  }
  return arrObj
}
